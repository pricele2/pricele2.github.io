---
title: Working with US Census Data 
subtitle: A Friendly Introduction to API Workflows
date: 2026-06-24
author: Lauren Price

format: 
  revealjs:
      theme: [serif, styles.scss]
      slide-number: true
editor: visual
toc: false
---

## Hello Baker Ripley!

::: r-fit-text
**THANK YOU** for having me!

Data and policy leader with over a decade of experience using analytics, data science, and strategic planning to support decision-making in large K-12 school systems

My work sits at the intersection of education policy, finance, data science, and program strategy.

Practicum placement at Martha O'Bryan Center in Nashville — very similar to BR
:::

![Lauren at Posit::Conf 2022](/images/positconf2022-selfie.JPG){fig-align="left" width="375"}

::: notes
On a personal angle, I earned my degrees from Notre Dame and Vanderbilt

My extended family hails from both New Jersey and New Mexico

I’ve lived in six states, including Texas for twelve years after grad school

Diversity Scholarship to Posit::Conf(2022) in DC
:::

## Before We Start

**Definition:**

An application programming interface (API) is a connection between computers or between computer programs. A type of software interface.

**Materials:**

Slides and code at <https://pricele2.github.io/blog/census-workshop/>

Plain text API key registered to Baker Ripley

Practice CSVs at [my Google Drive URL](https://drive.google.com/drive/folders/1XAhhvJAgn961SCvWq33m_gE9SSNdR9k7?usp=sharing)

[Optional Resource:]() Kyle Walker's *Analyzing US Census Data: Methods, Maps, and Models in R* (CC-BY-NC 2023)

Searchable [Glossary at Census.gov](https://www.census.gov/glossary/)

## Federal Responsibility

::: r-fit-text
Constitution requires a headcount of *all persons* every 10 years

- Article 1 Section 2: US House seats

- 1790: First national count

- 1902: Permanent agency

Present-Day:

- Dozens of surveys and statistical products

- Within the Department of Commerce

- Over 4,000 employees
:::

::: notes
<!-- By default a revealjs slide is 1050px wide by 700px tall. -->

Sources: https://www.census.gov/programs-surveys/sis/about/students101.html

TRANSITION: LOTS of data! Takes over 4000 employees to manage it all

Can be pretty overwhelming
:::

## Census Data Contains Multitudes

Sometimes mundane, sometimes controversial

Who counts as a person? As a citizen?

- Slavery, citizenship 

- Indigenous Peoples and LGBTQ+ civil rights

- Equitable representation

- Door-to-door "enumeration" for Decennial

- Statistical "fuzzing" through noise injection

## Census Data Serves Many Purposes

Census administers over 130 different surveys ("data products")

Each has its own life cycle and characteristics

Drives annual distribution of **more than \$675 billion** in federal funds to local, state, and tribal governments

## Three Data Products

**Decennial Census of Population and Housing (10-Year).**

- As required by US constitution, used for redistricting

**American Community Survey (Annual).**

- The social and economic needs of communities, across many demographic characteristics

**Small Area Income and Poverty Estimates (Annual).**

- Income and poverty statistics for all school districts, counties, and states

So! What do we need to know to get started? 

## Geographies Start with Blocks

[![Census geographies are built from Blocks, aggregated to Counties to States](/images/census-work/census-geo-levels.jpg){fig-alt="Hierarchy of US Census geographies, with Nation at the top and Blocks at the very bottom" fig-align="center" width="757"}](https://www2.census.gov/geo/pdfs/reference/geodiagram.pdf)

## Our Census Location, Mapped

Let's look up the [Census Geographies for Baker Ripley Central](https://geocoding.geo.census.gov/geocoder/geographies/onelineaddress?address=4450%20Harrisburg%20Blvd%2C%20Houston%2C%20Texas&benchmark=4&vintage=4)

**GEOID:** Unique ID for each census block, 15 digits. **48 201 310400 3032**

![4450 Harrisburg Blvd](/images/census-work/block-group-tract-BR.jpg){fig-alt="Greyscale line map from data.census.gov showing a highlighted section of city blocks" fig-align="left" width="685"}

## Blocks, Groups, Tracts

::: r-fit-text
**FIPS:** Five-digit code for each state (**48 Texas**) and county (**201 Harris**).

**Tract:** Smaller than a county: think *neighborhood*. Contains block groups.

- Ideally contain about 4,000 people and 1,600 housing units.

- Harris County has 1,115 Census tracts (2020 DHC) <!-- # Tract 310400 -->

**Block Group:** A subdivision of a census tract. Contains blocks.

- Generally between 600 — 3,000 people and 240 — 1,200 housing units.

- Smallest unit for which the Bureau tabulates data for *any survey other than Decennial*.

- Harris County has 2,830 block groups <!-- - **Block Groups 1, 2, and 3** -->

**Block:** The smallest unit for which the Bureau tabulates *decennial* census data.

- In cities, many blocks correspond to individual city blocks bounded by streets.

- Harris County has 51,077 Census blocks <!-- - **Block 3032**  -->
:::

## Online Data Outside API

[Which Data Tool or Table Should I Use?](https://www.census.gov/programs-surveys/acs/guidance/which-data-tool.html)

[Pre-Built Profiles](https://data.census.gov/profile)

[Fact Sheets](https://www.census.gov/library/fact-sheets.html)

[Quick Facts](https://www.census.gov/quickfacts/)

[Survey Explorer](https://www.census.gov/data/data-tools/survey-explorer/)

[Data.Census.Gov](https://data.census.gov/)

Great for exploring what exists, or first-pass "find a variable" searches

## For Reflection

What *characteristics* of your clientele can be contextualized by Census data? 

- Young children in poverty (e.g., Head Start)

- Veteran status

- Small business needs, planning 

- Need-based eligibility (Utilites, etc.)

Almost every aspect of BR's programs can be contextualized with Census data.


## Plus Free Learning Resources:

::: r-fit-text
[Census Academy Courses/Webinars](https://www.census.gov/data/academy.html)

[Email Updates from Census](https://public.govdelivery.com/accounts/USCENSUS/subscriber/new)

[Textbook by TCU prof Kyle Walker](https://walker-data.com/census-r/) *Analyzing US Census Data: Methods, Maps, and Models in R* (CC-BY-NC 2023)

[National Historic Geographic Info System](https://www.nhgis.org/)

- Housed at the University of Minnesota

- Free, with account registration required 

- The National Historical Geographic Information System (NHGIS) provides easy access to summary tables and time series of population, housing, agriculture, and economic data, along with GIS-compatible mapping files, for years from 1790 through the present and for all levels of U.S. census geography, including states, counties, tracts, and blocks.
:::

## Why API? 

Many data resources already available -- why API? 

Professional tools for professional purposes

Reproducible, reusable code

Minimize chaos & navigation clutter

Point and click is not future-proof 

- Recent overhaul of data.census.gov

Do it for your future self

## Let's start

```{r}
#| label: install.library
#| eval: true
#| echo: true
#| code-overflow: wrap
#| output: false
# Do this extra step the first time
# install.packages(c("tidycensus", "tidygeocoder", "censusapi", "tidyverse", "mapview"))

library(tidycensus)
library(tidyverse) # dplyr and ggplot2
library(mapview)
library(censusapi)
library(tidygeocoder)
library(sf)

# Check if you already have a CENSUS_KEY or CENSUS_API_KEY saved
get_api_key() 

# If your key returns, no further action is needed
```

## Set Up Census API Key

```{r}
#| label: set.api.key
#| eval: false
#| echo: true
#| code-overflow: wrap
#| output: false
# If not, use the functions you've already loaded 

# To add your key to your R environment file
census_api_key("PASTEYOURKEYHERE", overwrite = FALSE, install = TRUE)

# Reload .Renviron and check again
readRenviron("~/.Renviron")

# Confirm that the expected key prints to Console
get_api_key() 
```

Give yourself a round of applause!!!

## "The Decennial Census" 

Let's look at the Demographic & Housing Characteristics (DHC) data

```{r}
#| label: load.vars
#| eval: true
#| echo: true
#| code-overflow: wrap
#| output: true

# Using the load_variables() function
# From the tidycensus package

vars_dhc = load_variables(2020, dataset = "dhc")

# View(vars_dhc)
```

Run `View(vars_dhc)` and sort by name

- Prefix H \~ Housing (683 variables)

- Prefix P \~ Population

In the chat: **What other patterns do you see?**

## Redistricting Dataset (PL)

Smaller dataset extracted from DHC for redistricting under Public Law 94-171 (PL)

The summary file tables include:

P1. – Race

P2. – Hispanic or Latino, and not Hispanic or Latino by Race

P3. – Race for the Population 18 Years and Over

P4. – Hispanic or Latino, by Race for the Population 18 Years and Over

P5. – Group Quarters Population by Major Group Quarters Type

H1. – Occupancy Status (Housing)

## **P1_001N** : Total Pop from DHC 

Let's write code to find the total population of a county.

By default, `get_decennial()` sets the arguments year = 2020 and sumfile = "pl".

What happens if you enter just "Harris" in the `county` field?

The `county` field accepts a string or FIPS code ("201").


```{r}
#| label: harris.blocks
#| eval: true
#| echo: true
#| code-overflow: wrap
#| output: false

# Function get_decennial() via tidycensus() package 

harris_pop_2020 = get_decennial(
  geography = "tract",
  variables = "P1_001N", # TOTAL POPULATION
  state = "TX",
  county = "Harris County", 
  sumfile = "pl", 
  year = 2020, 
  geometry = FALSE, # Toggle T/F for shapefiles
  show_call = TRUE) # Toggle T/F to print API call to console

```

## Harris County, 2020

Let's look at what returns from the API.
```{r}
#| echo: true
colnames(harris_pop_2020)
```

Remember, we asked for `tract` level data. 

Can we find the number of units (`geography = "tract"`) in the county?

```{r}
#| echo: true
length(unique(harris_pop_2020$GEOID))
```

And here is our total for variable `"P1_001N"`:

```{r}
#| echo: true

h_pop = harris_pop_2020 |> 
  group_by(variable) |> 
  summarise(pop = sum(value))

print(h_pop[1,])
```

What happens when you swap in `year = 2010`?

## **P001001** : Population from PL 2010

Different variable name for `Total Population` in 2010!

Census programmers: sometimes they're just like us.

```{r}
#| label: harris2010
#| echo: true
#| eval: true
vars_pl_2010 = load_variables(2010, "pl") # Let's look for it

harris_pop_2010 = get_decennial(
  geography = "tract",
  # variables = "P1_001N", # Varname from 2020 
  variables = "P001001",
  state = "TX",
  county = "Harris County", 
  sumfile = "pl", 
  year = 2010, 
  geometry = FALSE, 
  show_call = TRUE)

h_pop_2010 = harris_pop_2010 |> 
  group_by(variable) |> 
  summarise(n = sum(value))
```

What do you learn when you run `print(h_pop_2010[1,])`?

## Population in all 13 counties

13 counties served by Baker Ripley and Workforce Solutions

List at [https://www.wrksolutions.com/regional-economic-data/](https://www.wrksolutions.com/regional-economic-data/quarterly-employment-reports-by-county)

```{r}
#| label: gulfcoast.blocks
#| eval: true
#| echo: true
#| code-overflow: wrap
#| output: true

# Another tool is tidycensus::data()
fips_codes = data("fips_codes")

# Let's make a string of the FIPS codes of counties served 
ws_gulf_coast = c("015", "039", "071", "089", "157", "167", "291", "321","339", "471", "473", "481", "201") 

# Run the API call using the string of FIPS codes as the target 
gulf_coast_pop_2020 = get_decennial(
  geography = "block group", 
  variables = "P1_001N", # TOTAL POPULATION
  state = "TX", 
  county = ws_gulf_coast,
  sumfile = "dhc", year = 2020, geometry = FALSE, 
  show_call = TRUE)

all_pop = gulf_coast_pop_2020 |> group_by(variable) |> 
  summarise(pop = sum(value))  # 7,297,022

print(all_pop[1,])
```

## Try Housing Occupancy, by County

Take a simple example, with Occupied or Vacant

Here, using the DHC file and variable names.

```{r}
#| label: housing.map
#| eval: true
#| echo: true
#| code-overflow: wrap
#| output: false

## 13 Counties served by Workforce Solutions
gulf_housing_2020 = get_decennial(
    geography = "county", 
    variables = c("H3_002N", "H3_003N"), # Occupied, Vacant
    summary_var = "H3_001N", # total Housing Units
    state = "TX", 
    county = ws_gulf_coast,
    sumfile = "dhc", 
    year = 2020, 
    geometry = TRUE, # Changed from FALSE
    show_call = TRUE) |> 
  mutate(pct = 100 * (value /  summary_value)) 

colnames(gulf_housing_2020)

```

Once we have the dataset, we plot from the `geometry = TRUE` in the next step:

```{r}
#| label: housing.map2
#| eval: true
#| echo: true
#| code-overflow: wrap
#| output: true
#| output-location: slide
# Note: the mutate / value / summary syntax here is specific to tidycensus
# Otherwise would require a group_by() summarize() situation

ggplot(gulf_housing_2020, aes(fill = pct, color = pct)) + 
  geom_sf() + 
  facet_wrap(~ variable)
```

## Let's Clean Up The Map

```{r}
#| label: housing.map3
#| eval: true
#| echo: true
#| code-overflow: wrap
#| output: true

gulf_counties2 = gulf_housing_2020 |> 
  rename(summary_value, "Housing Units" = summary_value) |>
  mutate(housing_status =
    case_when(
      variable == "H3_002N" ~ "Occupied", 
      variable == "H3_003N" ~ "Vacant"
    )) |> 
  select(-variable)
```

Rename items, and then reverse-code the fill and color

```{r}
#| label: housing.map4
#| eval: true
#| echo: true
#| code-overflow: wrap
#| output: true
#| output-location: slide

ggplot(gulf_counties2, 
       aes(fill = -pct, color = -pct)) + 
  geom_sf() + 
  facet_wrap(~ housing_status)
```

## Brainstorm Time

From earlier: What data questions could be relevant to Baker Ripley programs & mission?

What timepoints are relevant to these questions?

DCH and PL are good datasets for 2020 points in time

What about *data since then?!*

## ACS and Population Changes

**American Community Survey (Annual).** The social and economic needs of communities, across many demographic characteristics.

Language spoken at home, education, commuting, employment, mortgage/rent status, income, poverty and health insurance coverage.

[Subjects in the ACS](https://www.census.gov/programs-surveys/acs/guidance/subjects.html)

[Five Year vs One Year Estimates](https://www.census.gov/programs-surveys/acs/guidance/estimates.html)

<!-- [Which Data Tool or Table Should I Use?](https://www.census.gov/programs-surveys/acs/guidance/which-data-tool.html) -->

## Tradeoffs: Recency and Precision

The ACS is the only source of local statistics for most of the 40-plus topics it covers.

Functions in two packages will call data from ACS: 

- `tidycensus::get_acs()` also handles ZIP or ZCTRA inputs

- `censusapi::getCensus()` will retrieve from *any Census API with its programmatic name*

## SAIPE and Children

**Small Area Income and Poverty Estimates (Annual).** Income and poverty statistics for all school districts, counties, and states.

- Not included in `tidycensus` functionality

- Is included in `censusapi` toolkit, through `getCensus()`

  `"timeseries/poverty/saipe"` \# States and Counties, through 2024

  `"timeseries/poverty/saipe/schdist"` \# Districts, through 2024

## Datasets through censusapi()

Census publishes approximately 1,600 API endpoints!

You will probably need to do some sleuthing in the [documentation](https://www.census.gov/data/developers/data-sets.html)

```{r}
#| label: list.census
#| eval: true
#| echo: true
#| code-overflow: wrap
#| output: false

censusapi::listCensusApis()

local_api_list = listCensusApis() 
```

What data questions could be relevant to Baker Ripley programs & mission?

```{r}
#| eval: true
#| echo: true
View(local_api_list)

getOption("max.print")

options(max.print = 50000)
```

What can we reasonably conclude about the NAs in the `vintage` field?

## So many data elements and time points!

Metadata to the rescue! `listCensusMetadata`

Critical argument is `type`:

- "geographies", or "groups", or "variables" (use include_values = TRUE)

- Confirm here that Block-Level is for the Decennial (PL or DHC) survey only

```{r}
#| eval: false
#| echo: true
vars_acs5 = listCensusMetadata(
  name = "acs/acs5",
  vintage = 2024,
  key = NULL,
  type = "variables",
  # group = NULL,
  # variable_name = NULL,
  # include_values = FALSE
)
```

## Need to know where you're looking

`"What are some characteristics of our neighborhoods?"`

Requires knowing the census location of the programs!

## Geocode One Location for ACS

[Find Geographies for One Address](https://geocoding.geo.census.gov/geocoder/geographies/onelineaddress?form)

- State, County, Tract, Block Group

## Geocode with `tidygeocoder::geo()`

The geocode function utilizes this function on addresses contained in dataframes.

Let's do it!

```{r}
#| label: geocode.one
#| eval: true
#| echo: true
#| code-overflow: wrap
#| output: true

library(sf) #Handles shapefiles

baker_r <- geo("4450 Harrisburg Blvd, Houston TX") 

baker_r_sf <- st_as_sf(baker_r, coords = c("long", "lat"),
  crs = 4326) # Coord Reference System

br_5km <- baker_r_sf |>  st_buffer(5000) 
```

```{r}
#| label: geocode.one.mapview
#| eval: true
#| echo: true
#| code-overflow: wrap
#| output: true
mapview(br_5km) + mapview(baker_r_sf)
```

## Let's see their neighbors

Since now we have an input area representing the 5 kilometers around the address, we can pass this as the `filter_by` argument in `get_decennial()` or `get_acs()`.

This gives back the Census blocks within 5km (5000 meters) of the address without any additional need for spatial overlay.

```{r}
#| label: pull.blockpop.dhc
#| eval: true
#| echo: true
#| code-overflow: wrap
#| output: true

block_pop_brc <- get_decennial(
  geography = "block",
  variables = "P1_001N",
  state = "TX",
  county = "Harris County",
  geometry = TRUE,
  filter_by = br_5km,
  sumfile = "dhc",
  year = 2020
) 
```

## Map!

```{r}
#| label: pull.blockpop.dhc2
#| eval: true
#| echo: true
#| code-overflow: wrap
#| output: true

mapview(block_pop_brc)

sum(block_pop_brc$value) 
```

## Now let's try all BR Sites!

```{r}
#| label: geocode.brsites
#| eval: true
#| echo: true
#| code-overflow: wrap
#| output: false

all_hs = read_csv("~/R/pricele2.github.io/blog/census-workshop/data/all_ehs_hs_tx_locations.csv") |> as_tibble() |> 
  rename(Add1 = 6) 
  # |> mutate(Mail_Address = str_c(Add1, Add2, sep = " "))

colnames(all_hs)
```

## Next

```{r}
#| label: hs_geos3
#| eval: true
#| echo: true
#| code-overflow: wrap
#| output: true

hs_geos = geocode(  # Note the full word for the full dataset 
  all_hs, 
  # street = "Mail_Address",
  street = "Add1",
  city = "city",
  state = "state",
  postalcode = "zip",
  method = "census",
  full_results = TRUE,
  api_options = list(census_return_type = "geographies")
)

colSums(is.na(hs_geos)) #152 missing, probably need data cleaning
```

## Filter for `ws_gulf_coast` from earlier

```{r}
#| label: geocode.brsites.filter
#| eval: true
#| echo: true
#| code-overflow: wrap
#| output: true
#| 
hs_geos2 = hs_geos |> filter(state_fips == "48") |>  #drops the rows with "NA" returned
  filter(county_fips %in% ws_gulf_coast) 
  
hs_geos_sf = st_as_sf(hs_geos2, 
      coords = c("long", "lat"),
      crs = 4326) 
  
hs_all_5km <- hs_geos_sf |> st_buffer(5000) 

# mapview(hs_all_5km) + mapview(hs_geos_sf)
```

## What about Children Under 5 In Poverty?

```{r}
#| label: pull.blockpop.acs5
#| eval: true
#| echo: true
#| code-overflow: wrap
#| output: true

vars_acs = load_variables(2024, "acs5") # Searched for "Under 5"

block_pop_wsgc_u5_pov <- get_acs(
  geography = "tract",
  variables = c("B17001_004", "B17001_018"), 
  year = 2024, # Defaults to 2023 
  state = "TX",
  geometry = TRUE
  )  

str_length(block_pop_wsgc_u5_pov$GEOID[1]) # 11

mapview(block_pop_wsgc_u5_pov)

sum(block_pop_wsgc_u5_pov$estimate) # 388,508 across Texas
```

If you wanted to filter for only those tracts within 5KM of location in the 13 counties... how would you go about it?

```{r}
#| echo: false
#| eval: false
# colnames(block_pop_wsgc_u5_pov) # GEOID is 12 chars
# 
# str_length(block_pop_wsgc_u5_pov$GEOID[1])
# 

# hs_all_5km_2 = hs_all_5km |> 
# mutate(geoid = str_c(state_fips, county_fips, census_tract, census_block, sep ="")) 
# 
# str_length(hs_all_5km_2$geoid[1])
# 
# hs_all_5km_2 = hs_all_5km |> 
# mutate(geoid = str_c(state_fips, county_fips, census_tract, census_block, sep ="")) |> 
#   mutate(GEOID = str_sub(geoid, 1, 12))
# 
# str_length(hs_all_5km_2$GEOID[1])
#   
# block_pop_wsgc_u5_pov2 = block_pop_wsgc_u5_pov |> 
#   filter(GEOID %in% hs_all_5km_2$GEOID)

```

## Make a Work Plan

- What do I want to learn? What data product holds relevant elements?

- What time point?

- What geographic level is available for this?

## In Conclusion

- Kyle Walker's classes and resources are superb (TCU Professor)

- Map mockups or "a draft for feedback" will save you time!

- Embrace the nerdery!

**Thank you!**

- Find me on [LinkedIn](https://www.linkedin.com/in/lauren-price-data/)

<!-- ## Behavior Risk Factor Surveillance System (BRFSS) -->

<!-- ## Health Datasets by Tract  -->

<!-- Glossary at https://data.cdc.gov/Survey-Data/Behavior-Risk-Factor-Surveillance-System-BRFSS-Glo/5amh-5sx3/about_data -->
